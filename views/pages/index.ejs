<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Noise Maker</title>
  <style>
    #body {
      font-family: 'Arial', sans-serif;
      background: #e2e8f0;
      margin: 0;
      padding: 0;
      color: #333;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    #container {
      max-width: 400px;
      width: 100%;
      padding: 30px;
      background: #ffffff;
      border-radius: 12px;
      box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
      text-align: center;
    }

    #header {
      margin-top: 0;
      color: #2d3748;
      font-size: 2.2em;
      margin-bottom: 20px;
      font-weight: 700;
    }

    #form {
      display: flex;
      flex-direction: column;
      gap: 15px;
    }

    #input {
      padding: 14px;
      font-size: 16px;
      border: 1px solid #d1d5db;
      border-radius: 8px;
      outline: none;
      transition: border-color 0.3s, box-shadow 0.3s;
    }

    #input:focus {
      border-color: #4f46e5;
      box-shadow: 0 0 8px rgba(79, 70, 229, 0.3);
    }

    #button {
      padding: 14px 20px;
      font-size: 16px;
      color: #ffffff;
      background-color: #4f46e5;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: background-color 0.3s, transform 0.3s;
    }

    #button:hover {
      background-color: #4338ca;
      transform: translateY(-2px);
    }

    #link {
      display: inline-block;
      margin-top: 20px;
      font-size: 16px;
      color: #4f46e5;
      text-decoration: none;
      transition: color 0.3s;
    }

    #link:hover {
      color: #4338ca;
      text-decoration: underline;
    }
  </style>
</head>
<body id="body">
  <div id="container">
    <h1 id="header">Noise Maker</h1>
    <form id="form" action="/add" method="POST">
      <input id="input" type="text" name="name" placeholder="Enter a name" required>
      <button id="button" type="submit">Add Name</button>
    </form>
    <a id="link" href="/list">View Names List</a>
  </div>
</body>
</html>
